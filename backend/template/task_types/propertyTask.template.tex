
\begin{tabularx}{\textwidth}{<#~ 'c'.repeat(it.columns) #>X}
\blacktoprule

\multicolumn{<#~ it.columns + 1 #>}{p{\textwidth}}{
\ifthenelse{\equal{\sprache}{de}}
{Für jede richtige Zeile gibt es <#~ it.pointsPerCorrect #> Punkt(e). Für falsche Zeilen gibt es keine Negativpunkte.}
{You get <#~ it.pointsPerCorrect #> point(s) for each correct row. You don't get negative points for wrong rows.}
} \\

<# for (let i = 0; i < it.header.length - 1; i++) { #>
\ifthenelse{\equal{\sprache}{de}}{<#~ it.header[i].A #>}{<#~ it.header[i].B #>} &
<# } #>
\ifthenelse{\equal{\sprache}{de}}{<#~ it.header[it.header.length - 1].A #>}{<#~ it.header[it.header.length - 1].B #>} \\

<#- for (let i = 0; i < it.lines.length; i++) { /* for lines */ -#>
<# const line = it.lines[i] -#>

\addlinespace
\escapedStringTypeout{VSEXAM: {'Typ':'PropLine'
<#- for (let j = 0; j < line.options.length; j++) { /* for options */ -#>
, 'Antwort<#~ j + 1 #>': '<#~ line.options[j] ? "w" : "f" #>'
<#- } /* end for options */ -#>
}}

<#- if (it.solution) { -#>

<#- line.options.forEach((option) => { /* for options 2 */ -#>
<#~ option ? "{\\color{red} $\\tickedbox$} & " : "{\\color{red} \\emptyb} & " -#>
<#- }) /* end for options 2 */ -#>

<#- } else { #>
<#~ '\\emptyb & '.repeat(line.options.length) #>
<# } -#>
\ifthenelse{\equal{\sprache}{de}}{<#~ line.text.A #>}{<#~ line.text.B #>} \\

<#- } /* end for lines */ #>

\blackbottomrule
\end{tabularx}
