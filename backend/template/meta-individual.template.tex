% Variables for each individual exam
%
\newcommand{\zeigeloesung}{<%= it.zeigeloesung %>} % set to yes/no, no: leere zeilen werden gedruckt, yes, die lösung wird gedruckt
\newcommand{\sprache}{<%= it.sprache %>} % set to de/en, defaults to de
\newcommand{\randomexamnumber}{<%= it.randomexamnumber %>} % LS: Will be set by script
\newcommand{\sequenznummer}{<%= it.sequenznummer %>} % (CB) Wenn gesetzt, dann wird die Sequenznummer für personalisierte Klausuren
\newcommand{\vollername}{<%= it.vollername %>} % (CB) Wenn gesetzt, dann wird das Namensfeld der Klausur vorausgefüllt
\newcommand{\matrikelnummer}{<%= it.matrikelnummer %>} % (CB) Wenn gesetzt, dann wird das Matrikelnummerfeld der Klausur vorausgefüllt
