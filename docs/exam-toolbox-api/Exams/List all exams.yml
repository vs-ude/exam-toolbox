info:
  name: List all exams
  type: http
  seq: 1
  tags:
    - Exams

http:
  method: GET
  url: "{{baseUrl}}/api/exams"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Exam list
    request:
      url: "{{baseUrl}}/api/exams"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            {
              "_id": "",
              "courseName": "",
              "examinerName": "",
              "semester": "",
              "date": "",
              "examLengthMinutes": 0,
              "points": 0,
              "pageCount": 0,
              "conceptPages": 0,
              "lastEditedBy": "",
              "updatedAt": ""
            }
          ]

docs: Returns metadata for every exam stored in the database.
