info:
  name: Create exam
  type: http
  seq: 2
  tags:
    - Exams

http:
  method: POST
  url: "{{baseUrl}}/api/exams"
  body:
    type: json
    data: "{}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Created
    request:
      url: "{{baseUrl}}/api/exams"
      method: POST
      body:
        type: json
        data: "{}"
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "message": "",
            "insertedId": ""
          }

docs: Persists a new exam. The `lastEditedBy` field is set from the JWT.
