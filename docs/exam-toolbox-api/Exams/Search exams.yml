info:
  name: Search exams
  type: http
  seq: 5
  tags:
    - Exams

http:
  method: GET
  url: "{{baseUrl}}/api/exams/search/:searchText"
  params:
    - name: searchText
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Matching exams
    request:
      url: "{{baseUrl}}/api/exams/search/:searchText"
      method: GET
      params:
        - name: searchText
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            {
              "_id": "",
              "courseName": "",
              "examinerName": "",
              "semester": "",
              "date": "",
              "examLengthMinutes": 0,
              "points": 0,
              "pageCount": 0,
              "conceptPages": 0,
              "lastEditedBy": "",
              "updatedAt": ""
            }
          ]

docs: Full-text search across exam metadata.
