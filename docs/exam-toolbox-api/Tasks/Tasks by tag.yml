info:
  name: Tasks by tag
  type: http
  seq: 4
  tags:
    - Tasks

http:
  method: GET
  url: "{{baseUrl}}/api/taskPool/tags/:tagId"
  params:
    - name: tagId
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Matching tasks
    request:
      url: "{{baseUrl}}/api/taskPool/tags/:tagId"
      method: GET
      params:
        - name: tagId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            {}
          ]

docs: Returns tasks that have the given tag assigned.
