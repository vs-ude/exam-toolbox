info:
  name: Search tasks
  type: http
  seq: 6
  tags:
    - Tasks

http:
  method: GET
  url: "{{baseUrl}}/api/taskPool/search/:questionText"
  params:
    - name: questionText
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Matching tasks
    request:
      url: "{{baseUrl}}/api/taskPool/search/:questionText"
      method: GET
      params:
        - name: questionText
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            {}
          ]

docs: Full-text search on task question text.
