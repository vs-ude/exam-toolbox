info:
  name: Add child task
  type: http
  seq: 11
  tags:
    - Tasks

http:
  method: PUT
  url: "{{baseUrl}}/api/taskPool/addChild/:taskId"
  params:
    - name: taskId
      value: ""
      type: path
  body:
    type: json
    data: "{}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Child task added
    request:
      url: "{{baseUrl}}/api/taskPool/addChild/:taskId"
      method: PUT
      params:
        - name: taskId
          value: ""
          type: path
      body:
        type: json
        data: "{}"
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "message": ""
          }

docs: Creates a child variant of an existing task and links it to the parent.
