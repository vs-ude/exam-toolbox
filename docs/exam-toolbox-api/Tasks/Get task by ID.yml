info:
  name: Get task by ID
  type: http
  seq: 8
  tags:
    - Tasks

http:
  method: GET
  url: "{{baseUrl}}/api/taskPool/:taskId"
  params:
    - name: taskId
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Task document
    request:
      url: "{{baseUrl}}/api/taskPool/:taskId"
      method: GET
      params:
        - name: taskId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: "{}"
  - name: 404 Response
    description: Not found
    request:
      url: "{{baseUrl}}/api/taskPool/:taskId"
      method: GET
      params:
        - name: taskId
          value: ""
          type: path
    response:
      status: 404
      statusText: Not Found
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "message": ""
          }
